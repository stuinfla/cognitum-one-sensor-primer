.DS_Store
.vercel
node_modules/
*.log

# ruflo/AgentDB local runtime (per-machine memory, not source)
.swarm/
.claude-flow/
ruvector.db
.claude/memory.db

# personal purchase-history exports — never publish
docs/amazon-hardware-*

# KB MiniLM model cache (downloaded at build time; cached in CI, never source)
kb/models-cache/

# KB v1 backups (history lives in git)
kb/*.v1.rvf
kb/*.v1.json
kb/*.v1.rvf.idmap.json
kb/*.ids.v1.json
kb/*.meta.v1.json
kb/*.lock
