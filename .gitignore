.DS_Store
.vercel
node_modules/
*.log

# ---- Secrets & credentials — NEVER commit to this public repo ----
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets/
**/secrets.json
.mcp.json
.anthropic
*.apikey
api-keys*.json

# ---- Local agent runtime + our own records (per-machine, not source) ----
# The whole .claude/ dir is local Claude Code / claude-flow tooling + our memory records —
# never part of this public primer. Ignored wholesale (reversible if you want to share config).
.claude/
.swarm/
.claude-flow/
# Local Claude Code / Ruflo project config — dev tooling, not part of the public primer product.
CLAUDE.md
ruvector.db
agentdb.rvf
agentdb.rvf.lock
*.rvf.lock

# personal purchase-history exports — never publish
docs/amazon-hardware-*

# ---- KB download bundles — distributed via GitHub Releases (too large for the 100MB repo limit) ----
kb/*-kb-bundle.zip

# ---- KB build machinery & transient artifacts (regenerated, not source) ----
# MiniLM/bge model cache (downloaded at build time; cached in CI, never source)
kb/models-cache/
# big-variant embedding shards + temp files (deleted after ingest)
kb/*.vecs.*.jsonl
kb/*.vecs.*.jsonl.tmp
kb/*.tmp
# scratch helper scripts (prefixed with _)
kb/_*.sh
kb/_*.mjs
# archived v1 backups (history lives in git)
kb/_archive/
kb/*.v1.rvf
kb/*.v1.json
kb/*.v1.rvf.idmap.json
kb/*.ids.v1.json
kb/*.meta.v1.json
kb/*.lock
